#cloud-config
final_message: "Cloud init is done.  Woohoo!"
password: secret
chpasswd: { expire: False }
ssh_pwauth: True
hostname: {{ item.name }}
ssh_authorized_keys:
  - {{ ssh_public_key }}
mounts:
  - [ swap ]
